namespace EasyStock.Domain.Product.Enums
{
    public enum ProductUnit
    {
        Unit = 1,     // Unidade (ex: peças, caixas)
        Kilogram = 2, // Quilo
        Gram = 3,     // Gramas
        Liter = 4     // Litros (opcional)
    }
}
